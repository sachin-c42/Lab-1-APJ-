
 # program to input three numbers and find the maximum between three numbers using the conditional/ternary operator.

package Operatorsss;
import java.util.*;
public class maxnum {

	public static void main(String[] args) {
		Scanner scanner = new Scanner(System.in);

        // Declare variables to store the two numbers
        int num1, num2;

        // Input two numbers from the user
        System.out.print("Enter two numbers: ");
        num1 = scanner.nextInt();
        num2 = scanner.nextInt();

        // Find the maximum using the ternary operator
        int max = (num1 > num2) ? num1 : num2;

        // Output the result
        System.out.println("The maximum number is: " + max);

     
    }
}



package Operatorsss;
import java.util.*;
public class Shift {

	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);

        // Declare three variables to store the input numbers
        int num1, num2, num3;

        // Take input from the user
        System.out.print("Enter three numbers: ");
        num1 = sc.nextInt();
        num2 = sc.nextInt();
        num3 = sc.nextInt();


        int max = (num1 > num2) ? ((num1 > num3) ? num1 : num3) : ((num2 > num3) ? num2 : num3);

       
        System.out.println("The maximum number is: " + max);

     

	}

}

